extends base.pug

block content 
    h1 #{video.title} 
    h2 Description: #{video.description}
    h2 hashtag : #{video.hashtags}
    h3 #{video.meta.views} #{video.meta.views === 1 ? "view" : "views"}
    
    a(href = `${video.id}/edit`) Edit Video &rarr;